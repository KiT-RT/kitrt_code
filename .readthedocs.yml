version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - doxygen
    - graphviz

python:
  install:
    - requirements: doc/requirements.txt

sphinx:
  builder: html
  configuration: doc/conf.py

submodules:
  exclude: all

formats: []
